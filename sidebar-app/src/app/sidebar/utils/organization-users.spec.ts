import { OrganizationUsersUtils } from './organization-users';

describe('OrganizationUsersUtils', () => {
  it('should create an instance', () => {
    expect(new OrganizationUsersUtils()).toBeTruthy();
  });
});
